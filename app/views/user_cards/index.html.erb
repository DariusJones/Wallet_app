<% @user_cards.each do |uc| %>
<p>
	<%= link_to(uc.user.fname, user_path(uc.user.id)) %> 
	can use
	<%= link_to(uc.card.card_type, card_path(uc.card.id)) %>
	whenever.
</p>

<!-- 	<p><%= uc.user.fname %> can use <%= uc.card.number %> whenever.</p>
 -->	
	<% end %>